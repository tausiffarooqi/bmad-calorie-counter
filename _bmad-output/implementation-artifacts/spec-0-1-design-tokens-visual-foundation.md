---
title: 'Design Tokens & Visual Foundation'
type: 'feature'
created: '2026-09-19'
status: 'done'
route: 'dispatch'
review_loop_iteration: 0
context: []
baseline_commit: 'ca6313c60b708d3feb6f07ff6611c3498fde8bfa'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** No project exists yet, and there is no shared visual foundation (color tokens, typography, radius scale, shadow-free elevation) for later screens to build on — without it, each screen would invent its own styling, guaranteeing visual inconsistency across the app.

**Approach:** Scaffold a Next.js 16.3.5 (App Router) project on Node.js 24 / TypeScript 6.0.3 / Tailwind CSS 4.3.3, initialize shadcn/ui as the component foundation, and configure the Muted Earth Editorial design tokens — 12 shadcn color-token overrides, Inter + italic-Lora typography roles, the sm/md/lg/full radius scale, and the no-drop-shadow elevation rule — as global theme values every later screen inherits automatically rather than hardcoding its own.

**Decision (resolved 2026-09-19):** TypeScript downgraded from Architecture's original 7.0.2 pin to 6.0.3, because `typescript-eslint` doesn't support TS7 yet (upstream fix expected in typescript-eslint's TS 7.1+ support, not yet released) and lint must work for every story going forward. `next.config.ts`'s `experimental.useTypeScriptCli` flag is removed — it was only needed for TS7. `ARCHITECTURE-SPINE.md` is being updated to match. Revisit upgrading back to TS7 once typescript-eslint ships support.

</frozen-after-approval>

## Code Map

- `package.json` -- scaffolded by `create-next-app@16.3.5`, then pinned to exact Architecture versions (typescript 6.0.3 — downgraded from 7.0.2, see Decision above; tailwindcss/@tailwindcss/postcss 4.3.3); shadcn init added class-variance-authority, cn, lucide-react, radix-ui, shadcn, tw-animate-css
- `next.config.ts` -- plain config, no `experimental.useTypeScriptCli` (only needed for TS7, removed with the downgrade)
- `app/layout.tsx` -- swapped the scaffold's default Geist fonts for Inter (`--font-sans`) and italic Lora (`--font-recommendation`) via `next/font/google`
- `app/globals.css` -- shadcn Nova preset's default oklch neutrals overridden with the Muted Earth Editorial hex tokens (background/foreground/card/card-foreground/muted-foreground/border/input/ring/primary/primary-foreground/accent/accent-foreground); `--radius-sm/md/lg` changed from the preset's proportional `calc(var(--radius) * n)` formula to literal 8px/10px/12px since DESIGN.md's scale isn't a clean multiplier; popover/secondary/destructive left at preset defaults (per DESIGN.md, destructive is deliberately unused)
- `components/ui/button.tsx` -- shadcn-generated Button; fixed two mismatches against DESIGN.md's Button component spec: base radius was hardcoded `rounded-lg` (now `rounded-sm`, since DESIGN.md reserves `lg` for the Recommendation card only), and the `outline` variant used `bg-background` (now `bg-card`, matching DESIGN.md's Button (secondary) fill)
- `components.json`, `lib/utils.ts` -- shadcn init output, untouched
- `app/page.tsx` -- replaced the create-next-app marketing boilerplate with a minimal token-showcase page (budget number, entry row, recommendation card, both buttons) to visually verify the foundation; this is temporary and gets replaced by the real Daily view in Epic 3

## Implementation Notes

- Build (`npm run build`) succeeds cleanly: Next.js 16.3.5 + Turbopack, TypeScript 6.0.3, Tailwind CSS 4.3.3 compiles the Muted Earth Editorial tokens correctly.
- Discovered `npm run lint` hard-fails on `typescript-eslint does not support TS 7.0` while implementing — not anticipated by the original Intent/Approach, and it's a project-wide tooling gap (not scoped to this one story), so stopped implementation and asked the human, per the "stop and replan" rule, rather than picking a fix unilaterally.
- Human chose to downgrade to TypeScript 6.0.3 (see Decision above). Re-verified against the actual shipped config after the downgrade and after pinning `typescript` to an exact `6.0.3` (npm's default `^` prefix had crept back in): `npm run build` succeeds (TypeScript 6.0.3, no `experimental.useTypeScriptCli` needed) and `npm run lint` now passes cleanly (0 errors) — confirming the downgrade actually fixes the toolchain gap it was chosen to fix.

## Review Triage Log

- **[medium, patch]** Code Map (then lines 26-27) and Implementation Notes (then line 36) still described the pre-downgrade TypeScript 7.0.2 + `experimental.useTypeScriptCli` setup after the Decision block recorded the downgrade to 6.0.3; `epic-0-context.md`'s Requirements & Constraints had the same stale TS 7.0.2 description. Verified directly against `package.json`/`next.config.ts` — all three were stale. Fixed: rewrote all three to describe the actual shipped TS 6.0.3 config and recorded the post-downgrade build/lint re-verification above. (Blind Hunter + Verification Gap, same root cause.)
- **[low, patch]** Implementation Notes ended with "See Open Questions" after the Open Questions section had already been deleted per the resolved-entry rule — a dangling cross-reference. Verified: no such section existed in the file. Fixed by removing the phrase (folded into the same edit as the row above). (Blind Hunter)
- **[medium, patch]** Typography tokens were only half-wired: `app/layout.tsx`/`globals.css` wire the `--font-sans`/`--font-recommendation` font-*family* variables, but the `label` and `display-number` roles' size/weight/line-height/letter-spacing (defined in `epic-0-context.md`) had no reusable token — `app/page.tsx` hardcoded `text-[52px] font-bold leading-none` and `text-[17px] italic` as arbitrary values. This directly undercuts the story's own stated goal ("every screen inherits these tokens automatically ... rather than hardcoding"). Verified by reading `app/page.tsx` and `app/globals.css` together. Fixed: added Tailwind v4 compound `--text-label`/`--text-display-number`/`--text-recommendation` theme tokens (size + line-height + weight, verified current v4 syntax) and switched `page.tsx` to the resulting `text-label`/`text-display-number`/`text-recommendation` utility classes. (Blind Hunter)
- **[low, patch]** `components/ui/button.tsx`'s `xs`/`sm`/`icon-xs`/`icon-sm` size variants used `rounded-[min(var(--radius-md),Npx)]`, a holdover from shadcn's original proportional-radius formula. Since `--radius-md` is now a fixed `10px` literal (not `calc(var(--radius) * n)`), `min(10px, 10px)` and `min(10px, 12px)` both evaluate to `10px` regardless of the differing N — those four sizes render at 10px radius instead of the base's correct 8px (`rounded-sm`). Verified directly in `button.tsx` and by hand-evaluating the CSS `min()`. Fixed: replaced all four with `rounded-sm`, matching the base/default/lg sizes. (Blind Hunter + Edge Case Hunter, same root cause — Edge Case Hunter's suggested fix pointed at `--radius-sm` inside the same broken `min()` wrapper; using the plain utility class is the more direct fix since the wrapper itself is now pointless.)
- **[low, patch]** `ARCHITECTURE-SPINE.md` frontmatter carried an inline comment on a metadata value (`updated: '2026-09-19' # TypeScript downgraded...`) — redundant with the same fact already recorded properly in the spine's Deferred section, and a YAML frontmatter value is the wrong place for prose. Fixed: removed the inline comment. (Blind Hunter)
- **[low, patch]** 5 unused `create-next-app` boilerplate SVGs (`public/file.svg`, `globe.svg`, `next.svg`, `vercel.svg`, `window.svg`) were left in place after `app/page.tsx` was replaced and no longer references them. Verified: none appear in the new `page.tsx`. `app/favicon.ico` is NOT included in this — Next.js's App Router uses it automatically via file-based convention, no import needed. Fixed: deleted the 5 unused SVGs, kept `favicon.ico`. (Blind Hunter)
- **[low, patch]** `DESIGN.md`'s measured-contrast note didn't cover `muted-foreground` directly on `background` (only on `card`) — a combination `app/page.tsx` actually uses. Computed: 3.16:1, meaningfully lower than the documented `muted-foreground`-on-`card` ratio (3.45:1). The existing usage guidance ("keep to secondary/decorative labels") already covers this combination safely regardless, but the number itself was missing. Fixed: added the ratio to `DESIGN.md`'s Colors section. (Blind Hunter)
- **[false]** AGENTS.md's directive-sounding content ("committing it with your work keeps the tree clean") was flagged by both Blind Hunter and Verification Gap as worth independently confirming rather than trusting. Verified directly: `node_modules/next/dist/server/lib/generate-agent-files.js` exists in the installed Next.js 16.3.5 package and contains the exact `nextjs-agent-rules` marker AGENTS.md references — this is genuine, documented Next.js 16 framework-generated content (auto-regenerated by `next dev`), not injected or third-party content. No action needed.
- **[false]** Blind Hunter read the Code Map's dependency-pinning sentence as claiming all of `class-variance-authority`/`cn`/`lucide-react`/`radix-ui`/`shadcn`/`tw-animate-css` were "pinned to exact Architecture versions." Re-read the actual sentence: a semicolon separates "pinned to exact Architecture versions (typescript, tailwindcss)" from "shadcn init added [those six packages]" — the second clause never claims pinning, and none of those six packages are named in Architecture's Stack table at all. No contradiction exists; no action needed.
- **[low, rejected]** `components/ui/button.tsx` imports `cn` directly from the `"cn"` package rather than the project's `@/lib/utils` alias, making `lib/utils.ts`'s re-export currently unused. Verified: this is shadcn's own current CLI-generated pattern (I did not hand-write button.tsx's imports), not a mistake introduced by this story. Harm is negligible (one unused one-line file) and the "fix" is ambiguous without more evidence of whether future `shadcn add` components follow the same pattern — fighting the generator's own convention risks new inconsistency. Rejected: low impact, no simple direct fix available.
- **[low, rejected]** No global CSS rule (e.g. `* { box-shadow: none !important; }`) enforces DESIGN.md's "no drop shadows" rule — it currently holds only because nothing added a shadow yet (verified: zero `shadow-*` classes anywhere in the diff). A blunt global `!important` override risks side effects on future shadow-based focus rings. Rejected: no current violation exists to fix, and the speculative preventive fix carries its own risk.
- **[false]** Edge Case Hunter flagged no explicit `--radius-full` token for DESIGN.md's `full: 9999px` rounded scale. Verified: Tailwind v4 ships `rounded-full` → `9999px` as a built-in utility independent of any custom `--radius-full` theme token, and the original shadcn preset never defined one either — this was never needed and nothing regressed.
- **[low, deferred]** `sprint-status.yaml`'s status vocabulary (`backlog`/`ready-for-dev`/`in-progress`/`review`/`done`) has no state distinguishing "paused pending a human decision" from "actively progressing," which is what this story's mid-implementation stop-and-replan actually was. This is a sprint-planning tooling/vocabulary gap, not something this story's own intent (design tokens & visual foundation) covers or can fix. Deferred to `deferred-work.md`.
