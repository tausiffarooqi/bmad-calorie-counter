- source_spec: `_bmad-output/implementation-artifacts/spec-0-1-design-tokens-visual-foundation.md`
  summary: sprint-status.yaml's story status vocabulary (backlog/ready-for-dev/in-progress/review/done) has no state for "paused pending a human decision," which is what a mid-implementation stop-and-replan actually is.
  evidence: Story 0.1 hit a real stop-and-replan (typescript-eslint/TS7 incompatibility) and sat at "in-progress" throughout, indistinguishable from active work. This is a sprint-planning tooling/vocabulary gap, not something any one story's own intent can fix.
- source_spec: `_bmad-output/implementation-artifacts/spec-0-2-cross-cutting-interaction-accessibility-primitives.md`
  summary: app/globals.css has no trailing newline at EOF.
  evidence: Pre-existing since Story 0.1 (confirmed against that story's diff), not caused by Story 0.2. npm run lint passes either way (no eol-last-style rule configured); cosmetic, direct one-line fix whenever someone next touches the file.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-registration.md`
  summary: Duplicate-email detection in the registration route depends on an exact string match against live Supabase's error message, with no automated test guarding it.
  evidence: If Supabase's "User already registered" message or behavior changes, the code silently falls through to a generic error instead of the required inline email-field error. Manually verified once against the real local instance; the empty-identities fallback remains as defense-in-depth. Fixing properly needs a test runner, which this project doesn't have yet.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-registration.md`
  summary: No automated tests exist anywhere in this repo (confirmed via repo-wide search) — all Story 1.1 verification was manual, live testing against local Supabase.
  evidence: Registration now involves real DB writes, Auth calls, and a rollback path, all verified by hand this session. Introducing a test runner (Vitest/Playwright) is a project-wide decision bigger than any single story.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-user-registration.md`
  summary: supabase/config.toml (the CLI's own default from `supabase init`) enables several unused services (S3 storage protocol, vector/analytics, edge runtime, extra OAuth providers).
  evidence: Not controlled by this story's registration logic. Worth trimming later since it makes the architecture's noted local-vs-Hostinger-VPS Supabase version/config parity check noisier than necessary.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-user-login.md`
  summary: proxy.ts redirects to /login on any unauthenticated protected-route visit without preserving the originally-requested path (no `?next=` param), so post-login the user always lands on `/` rather than where they were headed.
  evidence: Zero user-visible impact today since `/` is the only protected route that exists. Revisit once Epic 2+ adds real protected routes (meal logging, history, etc.) — add a `next` search param on the redirect-to-login and read it back on successful login instead of hard-coding `window.location.href = "/"`.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-user-login.md`
  summary: proxy.ts's `supabase.auth.getUser()` call is unguarded — no try/catch, no timeout — so a Supabase Auth outage or slow response either throws an unhandled error or hangs every request in the app, including `/login` itself, and its `error` return value is silently discarded, making "no session" and "couldn't verify session" indistinguishable.
  evidence: Not exercised by manual testing against a healthy local instance. Needs a product decision (fail-open vs. fail-closed under an Auth-service outage, and whether to show a dedicated error state) that's bigger than this story's scope.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-user-login.md`
  summary: Login and Register both lack page-specific `<title>`/metadata (`export const metadata`), falling back to the layout's generic title.
  evidence: Cosmetic, pre-existing on both auth pages since Story 1.1; not a Story 1.2 regression. Fix both together whenever someone next touches either page.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-user-login.md`
  summary: Login's submit button relies on React state (`disabled={submitting}`) to block double-submission, which isn't synchronous — a very fast double-click/double-Enter before the disabled state commits could fire two concurrent `signInWithPassword()` calls.
  evidence: Narrow race, no observed real-world occurrence; existing `disabled` state substantially mitigates it. A synchronous ref-based lock would close the remaining gap if this is ever observed live.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-user-login.md`
  summary: Login uses a hard reload (`window.location.href`) after `signInWithPassword()` to dodge a session-cookie timing race with the proxy; Register still uses `router.push()` after its own signup flow, which looks like the same pattern at a glance.
  evidence: Investigated directly — not the same risk. Register's session cookie is set via the `Set-Cookie` header on the completed `/api/auth/register` HTTP response (guaranteed synced by the time `await fetch()` resolves) before `router.push()` ever runs. Login's cookie is set via the browser client's in-page `document.cookie` write inside `signInWithPassword()`, which is the actual documented risky pattern the hard-nav fix targets. Revisit Register only if the same "bounced back" symptom is ever observed live there — don't blindly copy the hard-nav fix without evidence it's needed.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-manage-daily-calorie-target-dietary-preference.md`
  summary: Preferences saves don't call `router.refresh()`, so a second browser tab or the back/forward cache could show a stale pre-save value with no indication the DB has since changed.
  evidence: PRD and EXPERIENCE.md both state this product is single-tenant/single-user with no multi-device-simultaneous-use target; narrow edge case for personal hobby use, not worth the added complexity now.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-manage-daily-calorie-target-dietary-preference.md`
  summary: `profiles.dietary_preference` has no DB-level CHECK constraint — an unrecognized value would be silently coerced to `non_vegetarian` for display and could be silently re-persisted as such if the user saves without touching the radio.
  evidence: Substantially mitigated by typing `updateDietaryPreference`'s parameter as the `DietaryPreference` union (closes the only current write path that could introduce a bad value); a DB-level CHECK/enum would be defense-in-depth beyond what AD-1's sole-writer discipline already provides.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-manage-daily-calorie-target-dietary-preference.md`
  summary: `app/(routes)/preferences/page.tsx` throws an unhandled `Error` (rather than a friendly error state) if an authenticated user's `profiles` row is ever missing.
  evidence: Genuinely defensive-only — Story 1.1 guarantees the row at registration, never deleted independently. No route in this codebase has an `error.tsx` boundary yet; adding one ad hoc here would be inconsistent rather than fixing a pattern. Revisit if this codebase ever adopts route-level error boundaries generally.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-manage-daily-calorie-target-dietary-preference.md`
  summary: Inline "Saved."/error confirmation elements (`role="status"`/`role="alert"`) are mounted only when needed rather than always present with toggled text, which some screen-reader/browser combinations may not reliably announce.
  evidence: Identical, already-shipped pattern in Login and Register's own inline messages — not a Story 1.3-specific regression. Fixing it here alone would create inconsistency; revisit for all three forms together if ever prioritized.
- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-manage-daily-calorie-target-dietary-preference.md`
  summary: API request bodies are validated by hand (XOR checks, manual type guards) with no shared schema library (e.g. zod) across the three routes that now exist (register, preferences) — an unexpected extra field is silently ignored rather than rejected, safe today only because service functions manually whitelist columns.
  evidence: Matches the codebase's existing hand-rolled validation convention everywhere; introducing a validation library is a project-wide decision bigger than any single story.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-log-a-meal-via-text.md`
  summary: The Log Entry dialog's double-submit guard is state-based (`if (status === "submitting") return`) rather than a synchronous ref, leaving a narrow window for two rapid clicks to both proceed.
  evidence: Textarea can't submit via Enter (multiline), and React's render timing makes two genuinely concurrent clicks unlikely in practice — same disposition given to the identical finding in Story 1.2's Login page.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-log-a-meal-via-text.md`
  summary: The new `entries` table has `isRLSEnabled: false`, relying entirely on the service-layer (AD-1) for per-user access control with no DB-level backstop.
  evidence: Mirrors the pre-existing, already-accepted pattern on `profiles` — not introduced by this story specifically. Closing it (enabling RLS with a per-user policy) is a security-hardening decision better made once across all tables than piecemeal per story.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-log-a-meal-via-photo.md`
  summary: `lib/compress-image.ts`'s Canvas-based compression doesn't read/normalize a photo's EXIF orientation tag before drawing, so a phone photo taken in portrait could theoretically be compressed and sent to Gemini sideways/upside-down on browser engines that don't auto-correct orientation during canvas draw.
  evidence: Browser behavior here is genuinely inconsistent across engines/versions (some auto-correct, some don't) rather than a clear-cut bug; a proper fix requires manual EXIF parsing and canvas-rotation logic, real code complexity for an MVP. The failure mode degrades estimate accuracy rather than causing a hard failure. Revisit if real-world testing on phone cameras shows this actually happening.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-log-a-meal-via-photo.md`
  summary: The compression-phase staleness fix (close-mid-compression, rapid double-pick) was verified by code review and single-pick live tests, but the exact race window (compress in progress, then close/re-pick before it resolves) could not be reliably reproduced via browser automation — real Canvas/JPEG compression on this hardware consistently finishes faster than a separate CLI-driven "close" action can be scripted in between.
  evidence: The fix itself (a `pickIdRef` staleness counter checked before any user-visible action) directly mirrors the already-proven `requestIdRef` pattern in `hooks/use-entry-submission.ts`, and no corruption or duplicate/mismatched entries were observed across several single- and rapid-pick live tests. Revisit with a deliberately slow-loading test image (or a manual phone test) if this is ever suspected live.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-view-logged-entries.md`
  summary: `dayBoundary()`'s DST "fall back" case (clocks repeat an hour) is unhandled — `zonedWallClockToUtc()`'s two-pass correction picks *some* consistent UTC instant for an ambiguous local wall-clock time, but which side of the fold isn't a deliberate, tested choice.
  evidence: Only matters if a region's fall-back transition hour coincides with the app's fixed 05:00 boundary; most common IANA zones transition between midnight and ~4am specifically to avoid business-hours disruption, making this low-probability. Documented directly in the code (day-boundary.test.ts) and covered by a spring-forward test, but no fall-back test exists since no correct answer is currently guaranteed. Revisit if a real report of an off-by-one-hour Entry attribution ever surfaces.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-view-logged-entries.md`
  summary: The Entries list only refetches on mount and when `refreshKey` changes (i.e., after a successful log) — a tab left open across the actual 5am Day boundary keeps showing the previous Day's list indefinitely with no self-correction.
  evidence: Fixing this needs a wall-clock timer/interval recomputing the boundary independent of any user action — real added complexity for a narrow use case (leaving the tab open across 5am specifically, not just across midnight). A manual reload always corrects it. Revisit if this proves to be a real usability complaint.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-view-logged-entries.md`
  summary: `isValidTimeZone()`/`dayBoundary()` validate a browser-detected IANA timezone string against the server's own bundled ICU/tzdata — a client on tzdata newer than the server's Node/ICU version (a new or renamed IANA zone) would get every `GET /api/entries` request rejected with no workaround, since the timezone is never stored/overridable (FR-14).
  evidence: Low-probability (both platforms are regularly updated) but a real, total, silent failure mode for an affected user. Partially mitigated: the rejection is now logged server-side (`console.error` in the route) for diagnosability. A full fix would need either a maintained IANA-zone allowlist or a graceful UTC fallback — the latter risks silently showing wrong-but-confident Day boundaries, which seemed worse than an explicit, now-logged error. Revisit if this is ever observed in practice.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-accessible-in-progress-announcement.md`
  summary: No shared derivation between each dialog's visible JSX and its `announcement` string in `log-entry-dialog.tsx`/`log-photo-dialog.tsx` — kept in sync by convention/comment only, not a single source of truth.
  evidence: Review finding (Blind Hunter), not a functional bug today — both are hand-verified identical, and the narrowing done in this story's review pass (mirroring only `role="status"` pending/success text) shrank the surface to 2 branches per dialog instead of 4. Revisit if a future change to either dialog's status copy drifts the two out of sync.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-accessible-in-progress-announcement.md`
  summary: `entries-list.tsx`'s own independent `<ul aria-live="polite">` (Story 2.4) is a third live region on the page that can announce concurrently with/race against the dialogs' success announcement (Story 2.5).
  evidence: Review finding (Blind Hunter), real but a cross-story coordination concern spanning two stories' components, out of Story 2.5's own frozen scope (which explicitly forbids touching state machines and never mentions `entries-list.tsx`). Revisit if a real double/garbled-announcement report surfaces; likely fix is consolidating to fewer live regions or staggering the Entries refresh after the success announcement has had time to be read.
- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-accessible-in-progress-announcement.md`
  summary: No automated test coverage for either dialog's announcement state machine (`announcement` string derivation, `LiveRegion` wiring).
  evidence: Review finding (Blind Hunter). Consistent with the project-wide, already-logged decision (Story 1.1) to not introduce a test framework yet; a future refactor of either dialog could silently break the mirroring with nothing to catch it.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-classify-entries-as-meal-or-snack-beverage.md`
  summary: Show each Entry's classification (Meal/Snack-Beverage) in the Entries list (`app/entries-list.tsx`), so the meal-slot/budget effect Stories 3.2–3.5 add is visibly legible rather than inferred from behavior alone (supersedes EXPERIENCE.md's original "no explanatory copy needed" framing for this specific detail — human-directed change).
  evidence: Split from Story 3.1 to keep that spec under the ~1600-token scope target — Story 3.1's classification engine (schema + rule-based/Gemini paths + route wiring) was already a full spec on its own once the hybrid mechanism was resolved. Build immediately after 3.1 lands, since it depends on the `classification` column and `GET /api/entries` response existing.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-classify-entries-as-meal-or-snack-beverage.md`
  summary: `app/api/entries/route.ts` has no automated test coverage for any of its failure paths (auth, estimation failure, entry-creation failure) — Story 3.1's new classifier-failure branch simply joins this pre-existing gap.
  evidence: Review finding (Verification Gap), filed disposition `defer`. Closing this needs a route-level test harness that doesn't exist yet for any behavior in this file across Stories 2.1–3.1; adding one as a one-off for just the classifier-failure branch would be disproportionate to this diff. Revisit once route-level testing is established generally.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-classify-entries-as-meal-or-snack-beverage.md`
  summary: Two sequential Gemini calls (estimate + classify) for photo-mode Entries now share the existing `maxDuration = 60` request budget with no per-call timeout.
  evidence: Review finding (Edge Case Hunter). Real risk in principle (an unusually slow pair of calls could approach the ceiling and be hard-killed ungracefully by the platform), but this session's own live verification showed both calls together completing well under 60s on the real flash-lite model, and a proper fix needs a new timeout/fallback policy (what should happen when it fires) that the current spec doesn't settle. Revisit if request volume or observed Gemini latency variance increases.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-classify-entries-as-meal-or-snack-beverage.md`
  summary: No cheap retry path after a photo-mode classification failure — the full, already-paid-for estimation is discarded and the user must resubmit the photo from scratch to retry just classification.
  evidence: Review finding (Blind Hunter), rejected in review triage because the only real fix (caching the estimation result to allow a classification-only retry) means changing this story's own frozen I/O matrix ("never guess a classification," no `entries` row on classifier failure) rather than a same-story patch. Logged here as a legitimate future enhancement — worth designing properly (server-side cache keyed by request, or similar) once Epic 3 is otherwise stable, not bolted on ad hoc.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-compute-remaining-calorie-budget.md`
  summary: `GET /api/entries`'s `remainingBudget` wiring (route → `profiles` lookup → `useDailyView` hook → `page.tsx` render) has no automated test coverage at any level, including the new missing-profile-row failure branch.
  evidence: Review findings (Verification Gap, Blind Hunter), both filed/routed as `defer`. Same pre-existing, already-logged gap as Story 3.1's deferred-work.md entry — `app/api/entries/route.ts` has no test file for any of its behavior across Stories 2.1–3.2. `EntriesApiResponse.remainingBudget` is typed optional, so a future regression dropping the field from the response would silently render as an indistinguishable-from-loading blank with nothing failing. Revisit once route-level testing is established generally.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-2-compute-remaining-calorie-budget.md`, extended by `spec-3-3-meal-slot-recommendations-by-time-of-day.md` and `spec-3-5-over-target-override.md`
  summary: None of the Remaining Calorie Budget `<p>`, the Recommendation cards, or the Over-Target banner in `app/page.tsx` have `aria-live`/`role="status"` wiring, unlike `EntriesList`'s `<ul aria-live="polite">` — screen readers get no announcement when the budget number, the set of Recommendation cards, or the Over-Target banner changes after logging an Entry.
  evidence: Review finding (Blind Hunter, across three stories), real accessibility gap. Not patched in isolation because Story 2.5's own review already found and deferred a "multiple independent `aria-live` regions on one page can race/conflict" concern for this same Daily view — adding more live regions piecemeal, one per new dynamic element, would compound the known problem rather than fix it. Revisit as one holistic Daily-view live-region strategy covering budget + entries + recommendations + the Over-Target banner together, not per-element patches.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-meal-slot-recommendations-by-time-of-day.md`, extended by `spec-3-4-after-10pm-conditional-recommendation.md`
  summary: `POST /api/entries`'s new `tz` validation gate, `GET`'s `recommendations` field's route→hook→page wiring, and now the `remainingBudget` argument's wiring into `getRecommendations()` at both call sites, all have no automated test coverage at any layer.
  evidence: Review findings (Verification Gap, Blind Hunter), all filed/routed as `defer`. Same established, repeatedly-confirmed gap every Epic 3 story since 3.1 has joined — no route/page/hook test infrastructure exists anywhere in this project. A broken `tz` gate (inverted condition) would fail every real submission undetected; a dropped/renamed `recommendations` key would silently render as "no recommendations" with every existing test green; a mis-wired `remainingBudget` argument (swapped for another in-scope number) would compile cleanly and silently change every after-10pm/before-5am recommendation decision. Revisit once route-level testing is established generally.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-meal-slot-recommendations-by-time-of-day.md`
  summary: `POST /api/entries`'s response includes `remainingBudget`/`recommendations` (per FR-9's explicit contract) that the current frontend never consumes — `useEntrySubmission` only reads `calories`, and the Daily view gets its own copy from the follow-up `GET` triggered by `bumpRefreshKey()` after the dialog closes.
  evidence: Review observation (Blind Hunter, Verification Gap), not a defect — FR-9 explicitly requires the response to contain this data regardless of current client consumption. Logged as a future optimization: wire the dialogs/`useEntrySubmission` to consume `POST`'s own `remainingBudget`/`recommendations` directly (e.g. passed through `onSuccess`) instead of relying on a second `GET` round-trip, if reducing post-log latency or redundant DB reads ever becomes a real concern.
- source_spec: `_bmad-output/implementation-artifacts/spec-3-5-over-target-override.md`
  summary: The Daily view's Over-Target banner/Recommendation-card branching in `app/page.tsx` (the `isOverTarget` derivation and the mutual-exclusivity render logic) has no automated coverage anywhere — a wrong threshold (e.g. `<= 0` instead of `< 0`) or non-exclusive rendering would ship with `npm test`/`tsc`/`eslint`/`next build` all passing.
  evidence: Review finding (Verification Gap), filed/routed as `defer`. No component/e2e test harness exists for `app/page.tsx` or any `.tsx` file in this project — the one manual walkthrough recorded in Story 3.5's own spec doesn't re-run on future changes. Same class of gap as the route-level testing entries above; revisit if/when component-level testing is ever established for this project.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-first-login-prompt-remaining-budget.md`, extended by `spec-4-4-pre-10am-breakfast-offer.md`
  summary: `lib/services/profiles.ts`'s new `checkAndMarkFirstLoginPrompt()` has no automated test for its day-boundary comparison logic (null-vs-old timestamp, just-before/after 5am, DST). Story 4.4 adds a second, structurally identical case: `breakfastOfferAcceptedToday()` is a pure, dependency-free day-boundary comparison (no DB call at all) yet still can't be unit-tested in isolation, because importing anything from `profiles.ts` pulls in `lib/db/client.ts`, which throws at module-load time if `DATABASE_URL` isn't set for the plain `node --test` process (it isn't, since `node --test` doesn't load `.env.local` the way Next.js does).
  evidence: Matrix Test Audit (Story 4.4's step-03 implementation pass), real and structural, not a review finding this time. Same established, repeatedly-confirmed gap — no test/DB-mocking harness exists for any `profiles.ts` function or `route.ts` handler in this project. Unlike `day-boundary.ts`/`tone-message.ts` (deliberately dependency-free files, fully unit-tested), `profiles.ts` mixes pure helpers with DB-touching functions in the same file, so even its purely-computational logic inherits the module's DB import-time side effect. Fixing this cleanly would mean extracting the pure day-boundary-comparison helpers into their own dependency-free file (or introducing a DB-mocking harness) — bigger than any single story's scope. Revisit once route/DB-service-level testing is established generally, or as part of a small, dedicated "extract profiles.ts's pure helpers" refactor.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-first-login-prompt-remaining-budget.md`
  summary: `hooks/use-daily-view.ts` guards only against post-unmount `setState` (the `cancelled` flag), not response ordering — a fetch that resolves out of order could overwrite a fresher field (e.g. `showFirstLoginPrompt`) with a stale one.
  evidence: Review finding (Edge Case Hunter), real but a pre-existing characteristic of this hook since Story 3.2 — Story 4.1 only adds one more field riding the same existing pattern, not a new gap. First time this specific characteristic has been surfaced by review. Revisit with a request-ID guard (e.g. an incrementing ref compared before each `set*` call) if a real symptom is ever observed.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-first-login-prompt-remaining-budget.md`
  summary: `checkAndMarkFirstLoginPrompt()`'s read-then-write is not a compare-and-swap — two concurrent loads for the same account (e.g. a phone and a laptop open at once) could both see "not yet shown today" and both mark/show the First-Login prompt.
  evidence: Review findings (Blind Hunter, Edge Case Hunter), rejected in review triage because this is the frozen spec's own already-documented, deliberate tradeoff ("acceptable given single-user-at-a-time hobby scale") — worst case is a mildly redundant second prompt showing, no data corruption. Logged here since two independent review lenses raised it, worth reconsidering if multi-device usage ever becomes a real, stated use case for this app.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-tone-adaptive-daily-message.md`
  summary: `GET /api/entries`'s `showFirstLoginPrompt`-gated `toneMessage` computation (the yesterday-window fetch, `getToneMessageOutcome()` call, and the `page.tsx` render) has no automated test coverage at the route/hook/page layer — only the pure `tone-message.ts`/`day-boundary.ts` logic is unit-tested.
  evidence: Review findings (Verification Gap, Blind Hunter), both filed/routed as `defer`. Same established, repeatedly-confirmed gap every story since 3.1 has joined — no route/page/hook test infrastructure exists anywhere in this project. A regression that computed `toneMessage` unconditionally (dropping the `showFirstLoginPrompt` gate) or swapped the wrong Entries array into `getToneMessageOutcome()` would compile cleanly and pass every existing test. Revisit once route-level testing is established generally.
- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-decline-path-recommendations.md`
  summary: Tapping either `FirstLoginPrompt` button unmounts the prompt card + tone message and mounts a different subtree (budget header, `EntriesList`, Recommendation cards/Over-Target banner, Add Photo/Add Text buttons) with no explicit focus management — keyboard/screen-reader focus lands wherever the browser default puts it, not deliberately moved to the new content.
  evidence: Review finding (Blind Hunter), real and previously unnoticed — pre-existing since Story 4.1 introduced these buttons, not caused by Story 4.3 (which added no code). Distinct from the already-logged `aria-live` announcement gap (Stories 3.2/3.3/3.5's deferred entry) — this is about focus destination on unmount/mount, not live-region announcement of value changes. Revisit as part of the same future holistic Daily-view accessibility pass already planned for the `aria-live` gap, since both concern the same interaction surface.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-3-month-trend-view.md`
  summary: `GET /api/trends`'s own logic (401/400/500 branches and the window-start date arithmetic) and `app/(routes)/trends/page.tsx`'s fetch/render wiring have no automated test coverage at the route/page layer — only the pure `computeTrendDays()` aggregation is unit-tested.
  evidence: Review findings (Blind Hunter, Verification Gap — the latter explicitly declined to file it as a gap since it's the same established, project-wide convention, not something this diff broke), both converging on `defer`. Same repeatedly-confirmed gap every story has joined since 3.1 — no route/page test infrastructure exists anywhere in this project. Revisit once route-level testing is established generally.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-3-month-trend-view.md`
  summary: The new `/trends` page has no link back to the Daily view other than the browser back button — no back link/breadcrumb.
  evidence: Review finding (Blind Hunter), real but pre-existing — `/preferences` has the identical gap and predates this story. Noted as more noticeable now that a second, potentially-longer-content page exists without a return path. Revisit as a small, shared nav-affordance addition covering both `/preferences` and `/trends` together, not a per-page patch.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-3-month-trend-view.md`
  summary: `computeTrendDays()` calls `dayBoundary()` once per Entry (each constructing a fresh `Intl.DateTimeFormat` and running up to three format passes) rather than once per distinct day, and the trends route's `getEntriesForDay()` call selects every `entries` column for the full 3-month range instead of projecting only `calories`/`createdAt`.
  evidence: Review finding (Blind Hunter), real but negligible at this app's hobby/single-user scale (at most a few hundred Entries per 3-month window, computed once per page load) — matches the same "compute only when needed, not measured to matter" tradeoff already accepted for other performance findings in this project (e.g. Story 4.2's sequential yesterday-window fetch). The over-fetch is also a direct consequence of the frozen Boundaries' explicit decision to reuse `getEntriesForDay()` verbatim rather than add a new, narrower query variant. Revisit only if this app's data volume or usage pattern ever meaningfully changes.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-trend-summary-stats.md`
  summary: Story 5.1's already-accepted "current target only, not historized per day" simplification is inherited by the new aggregate stats, and amplified by them — changing the Daily Calorie Target today retroactively changes every past day's within/over-target classification and the "N of M days within target" headline, with no indication anywhere that the number reflects today's target applied backward rather than what the target actually was on each day.
  evidence: Review finding (Blind Hunter), real, not caused by this story's own code (the simplification itself is Story 5.1's, carried through unchanged) but newly worth flagging because a single aggregate headline is more likely to be taken at face value than a day-by-day list already showing each day's own (currently identical) target value. Revisit only if this app ever adds target-change history — genuinely out of scope for a Could-have reporting layer at this project's current stage.
- source_spec: `_bmad-output/implementation-artifacts/spec-5-2-trend-summary-stats.md`
  summary: The new stats `<div>` and the pre-existing day-row `<ul>` (Story 5.1) both carry `aria-live="polite"` and populate on the same render pass immediately after the fetch resolves — two adjacent live regions firing at once could announce/interleave oddly in some screen readers.
  evidence: Review finding (Blind Hunter), partially disputed in triage: `aria-live` announcing the first reveal of asynchronously-loaded content (transitioning from a loading skeleton to real content) is a legitimate, common use of live regions, not misuse — so the attribute's presence itself is not the defect. The "two regions firing simultaneously" interaction is real but implementation-dependent (varies by screen reader) and has no clean single-story fix (would need combining both into one region, a small redesign, not a patch). Revisit as part of the same future holistic Daily-view/Trends live-region strategy already noted in this project's other deferred `aria-live` entries (Stories 3.2/3.3/3.5).
- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-user-logout.md`
  summary: Logging out in one browser tab doesn't immediately propagate to other open tabs of the same app -- a second tab keeps rendering its (now-stale) authenticated Daily view until its own next network request happens to fail and redirect.
  evidence: Review finding (Blind Hunter), real but an inherent property of the cookie-based session model established since Story 1.2, not something Story 1.5 introduces -- the identical staleness would already occur if a session simply expired or was revoked server-side, with no logout involved at all. Fixing it would need a push-based auth-state sync (e.g. Supabase's `onAuthStateChange` wired across every client component, or a `BroadcastChannel`), new infrastructure disproportionate to this hobby-scale, effectively-single-user app. Revisit only if genuine simultaneous multi-tab/multi-device usage becomes common.
