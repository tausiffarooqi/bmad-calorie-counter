---
scope: Calorie Tracker MVP - all features/FRs in prd-bmad-calorie-counter-2026-09-15
purpose: build-substrate
altitude: feature
updated: 2026-09-19T17:57
---

- (decision) Working mode: Coaching path chosen by user
- (decision) Purpose/audience: spine is for the user's own build + as proof-of-practice artifact when submitting the prototype; no external collaborator audience -- purpose=build-substrate, no extra discussion/deck doc needed
- (version) Web-verified 2026-09-16: Next.js 16.3.5 current stable (Active LTS line); Node.js 24 current Active LTS (22 in Maintenance LTS, 26 not yet LTS until Oct 2026)
- (version) Web-verified 2026-09-16: Neon (Postgres-only, scale-to-zero, 100 free projects/0.5GB each) vs Supabase (full platform: Postgres+Auth+Storage+Functions bundled, free tier 2 projects/500MB, pauses after 1wk idle) -- trade-off for FR-20 auth + relational data needs
- (decision) Data store: plain self-hosted PostgreSQL (AD, adopted by user) -- dev connects to local laptop instance, later prod points to Hostinger VPS instance via env-configured connection string; no managed Postgres platform (Neon/Supabase) used
- (version) Web-verified 2026-09-16: Drizzle ORM now the more common 2026 default recommendation for TypeScript/Next.js (lighter, no codegen, edge-friendly) over Prisma (still solid, better migrations UX/gentler learning curve)
- (version) Web-verified 2026-09-16: Lucia deprecated March 2025 (now a learning resource, not a library); Auth.js frozen to security-patches-only under Better Auth; Better Auth is the current recommended library for new projects needing self-rolled email/password auth against Postgres
- (decision) Photo handling: no persistent blob storage -- photo Entry handled as transient in-memory upload straight into the estimation call, discarded after (FR-6); confirmed by user
- (constraint) Reality: Hostinger VPS runs self-hosted Supabase (Postgres+GoTrue Auth+Storage+PostgREST+Realtime via Docker), not plain Postgres; local laptop is plain Postgres only. User previously dropped managed Supabase free tier over low-usage pause warnings -- irrelevant once self-hosted (no pause behavior).
- (version) Web-verified 2026-09-16: Supabase CLI 'supabase init' + 'supabase start' brings up the full stack (Postgres, Auth, Storage, Realtime, Studio, API gateway) in Docker locally, CLI-managed compose, no hand-written Docker config; requires Docker Desktop; first run downloads images.
- (decision) Data + Auth platform (AD): standardize on self-hosted Supabase everywhere -- local dev via 'supabase init'+'supabase start' (Docker-managed full stack), prod via existing Hostinger VPS self-hosted Supabase. App uses Supabase Auth (GoTrue) for FR-20, not Better Auth or roll-your-own -- that question is now moot. Supabase Storage not used (FR-6 discards photos, no persistent blob storage needed).
- (decision) ORM: Drizzle (AD) -- lighter, no codegen, plain-SQL migrations against Supabase's underlying Postgres
- (decision) Open Question 8.1 resolved (AD): default EstimationProvider adapter is Gemini Flash (single multimodal LLM call: identify + estimate calories in one round trip), swappable behind the port per AD paradigm decision
- (decision) Deployment (AD): Next.js app deployed on Vercel; Supabase (Postgres+Auth) self-hosted on Hostinger VPS. User already runs this exact Vercel+Hostinger-Supabase pairing for another app, so this is a proven/familiar setup, not a first attempt.
- (event) All load-bearing decisions closed: paradigm (layered + EstimationProvider port), stack (Next.js 16.3.5/Node 24/TS/Tailwind), data+auth (self-hosted Supabase everywhere via CLI locally + existing Hostinger VPS in prod), ORM (Drizzle), estimation provider (Gemini Flash default), deployment (Vercel app + Hostinger Supabase). Proceeding to distill.
- (version) Web-verified 2026-09-16: TypeScript 7.0.2 (stable, 2026-07-08); Tailwind CSS 4.3.3 (stable, 2026-07-16); Gemini Flash current model id is gemini-3.8-flash (released 2026-09-02) -- supersedes the 'Gemini Flash' addendum reference which predates this release
- (event) Spine distilled to ARCHITECTURE-SPINE.md (6 ADs, stack, deployment/ERD/source-tree structural seed, capability map, deferred list)
- (event) All 4 review passes complete (reconcile-prd, rubric, version-verify, adversarial). Consolidated findings, triaging now.
- (constraint) CRITICAL (adversarial review): AD-2's estimate() had no shape for FR-4 insufficient-detail/retry outcome -- two AD-2-compliant implementations (throw vs return) would diverge. Fix: discriminated-union return type, FR-4 added to AD-2 Binds.
- (version) Web-verified 2026-09-16: gemini-3.8-flash benchmarked time-to-first-token ~13.3s at default/higher thinking_level -- directly threatens SM-1's <5s target. Google's own guidance: thinking_level='low' is recommended for latency-critical paths and is the fix (no hard token budget in Gemini 3.x, thinking_level enum replaces old thinking_budget int). Pinning thinking_level=low into AD-2's Rule as a required, easily-dropped config.
- (decision) New AD-7: Meal Slot fill state is derived from Entries (count of Meal-classified Entries this Day), never a separate persisted counter -- mirrors AD-5's precedent, closes adversarial finding #2 (FR-8/FR-10/FR-17 ambiguity).
- (decision) New AD-8: Recommendation content (FR-9/FR-19) is generated via a static, versioned rule-based lookup table (time-of-day slot x Dietary Preference x Over/Under-Target), not a second LLM call -- closes adversarial finding #3, and is reinforced (not just supported) by the gemini-3.8-flash latency finding: a single LLM call already strains SM-1, a second one cannot fit.
- (decision) AD-2 amended: classification (FR-7) is explicitly never an adapter concern -- estimate() returns description+calories only; entry-classifier service classifies Meal vs Snack/Beverage downstream from that output. Closes rubric+adversarial FR-7 ownership gap.
- (version) Web-verified 2026-09-16: Drizzle ORM latest npm version is 0.45.2 (1.0 beta line exists but not stable) -- pinning Stack row to 0.45.2 instead of unverified 'latest'. TypeScript 7.0.2 has a real Next.js 16.3.5 gotcha: TS7's npm package ships only the Go-native compiler (no lib/typescript.js JS Compiler API), so plain 'next build' fails to detect TypeScript unless next.config sets experimental.useTypeScriptCli:true -- noted as a scaffold-time setup requirement, not an architectural fork.
- (question) Open/deferred: self-hosted Supabase version on the Hostinger VPS was never checked against the local-dev Supabase-CLI version -- AD-3 assumes dev/prod parity; confirm both run compatible versions before relying on identical GoTrue/Postgres behavior across environments. Also: GoTrue requires SMTP configured (or email-confirmation disabled) for FR-20 signup to work out of the box -- decide at scaffold time.
- (event) Capability map fix: FR-21 (photo-upload-only UI notice) was miscategorized under AD-3 (auth) -- it's UI copy with no governing AD; corrected.
- (event) Reviewer fixes applied: AD-2 rewritten (retry shape, thinking_level=low, FR-7 classification ownership), new AD-7 (meal slot derived) and AD-8 (recommendation = static lookup), Stack table pinned (Drizzle 0.45.2), TS7/Next.js gotcha noted, Capability map FR-21 corrected, Deferred expanded (Supabase version parity, GoTrue SMTP). Lint clean (0 findings). Spine finalized.
- (direction) User relaxed SM-1: <5s is a soft target/nice-to-have, not a hard invariant. If Gemini's latency exceeds it, that's acceptable -- UI must show an in-progress/working indicator instead. Re-opens AD-2's hard latency framing.
- (version) Web-verified 2026-09-16: Vercel serverless function default maxDuration is 10s (Hobby plan) -- would silently truncate a slow Gemini call regardless of UI spinner. Configurable up to 60s standard, or 300s with Fluid compute, on Hobby. A synchronous request+spinner pattern is viable as long as maxDuration is explicitly raised; no async job-queue/polling architecture needed.
- (decision) AD-2 amended: SM-1 downgraded from hard-enforced binding to soft target; thinking_level='low' kept as sensible cost/speed default but no longer justified as required-to-avoid-a-violation. New AD-9 added: entry-submission route must explicitly set maxDuration (raise past Vercel's 10s default) and the client must show a persistent in-progress indicator for the duration of a pending estimate call.
- (event) Post-close amendment applied: AD-2/AD-8 de-coupled from SM-1 as a hard binding; new AD-9 (explicit Vercel maxDuration + in-progress UI indicator) added. AD IDs kept stable, none renumbered. Lint re-checked clean. Spine remains status:final.
- (decision) Confirmed: AD-9 stands as-is (synchronous request + explicit maxDuration + in-progress UI indicator). Considered fire-and-forget (waitUntil+polling/Realtime) and a real background queue (Vercel Queues/Inngest/etc) as alternatives; rejected for now -- waitUntil has no retry guarantee (verified via Vercel docs, doesn't actually remove failure risk) and a real queue is more infra than a solo-user prototype needs. Mobile-tab-backgrounding edge case left as a Deferred note with manual retry-the-log as the fallback, not a queue.
- (event) Deferred item 'GoTrue email-confirmation requirement' resolved by the downstream UX pass (2026-09-18): email confirmation disabled for this prototype. Back-ported into the spine's Deferred list as resolved.
- (constraint) Discovered during epics/stories planning (2026-09-19): Vercel serverless functions have a hard 4.5MB request-body limit (infrastructure-level, not bypassable via config). AD-4 routes photo bytes through the API route into memory for estimate(), so an uncompressed phone photo (routinely 3-8MB) could 413 outright, not just be slow.
- (decision) AD-4 amended: added client-side compression/resize requirement (cap longest dimension ~1600px, JPEG quality ~0.8) before upload, to stay safely under Vercel's 4.5MB body limit, plus a hard client-side rejection backstop if compression still leaves the file too large. AD-4 Binds extended to include FR-1/FR-2 (input mode/photo pipeline).
- (event) AD-4 amendment applied and lint re-checked clean. Spine remains status:final, updated 2026-09-19.
- (decision) Readiness-gate finding (sprint planning, 2026-09-19): AD-8's Recommendation lookup key includes Dietary Preference, but profiles.dietary_preference is nullable until the user visits Preferences (Story 1.3) -- undefined lookup case for a new user who logs a meal first. Resolved: profiles.dietary_preference defaults to 'non_vegetarian' at row creation (Story 1.1), changeable anytime in Preferences.
